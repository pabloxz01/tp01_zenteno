void setup() {
    size(400, 200);
  
    
    float c = 4;
    float b=2;
    float d=3;
  
    float resultado = (b+d)/(c+4);
  
    println("El resultado de la expresión (b + d) / (c + 4) es : " + resultado);
}
