void setup() {
    size(400, 200);
  
    float a = 4;
    float b = 5;
    float c = 1;
    
    float resultado = (b*a)- pow(b,2)/(4*c);
  
    println("El resultado de la expresión : " + resultado);
}
